; Per-directory PHP settings for the Photo Album app.
; PHP-FPM and CGI honour these automatically (cached for ~5 minutes).
; Increase limits if you need to upload larger originals.
upload_max_filesize = 20M
post_max_size       = 25M
max_file_uploads    = 20
